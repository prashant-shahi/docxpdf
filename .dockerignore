# Context should already contain apps/web/dist from `make build` / CI.
# Keep the build context small; only dist + nginx config are needed.
**
!apps/
!apps/web/
!apps/web/dist/
!apps/web/dist/**
!docker/
!docker/nginx.conf
!Dockerfile

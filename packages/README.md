# Shared Packages

| Package | Description |
|---|---|
| [`engine/`](engine/) | `@docxpdf/engine` — platform-agnostic document logic (types, migrate, shapes, DOCX builder, constants) |

## Usage

```typescript
import { buildDOCX, migrateDocumentData, PAGE_SIZES } from "@docxpdf/engine";
```

Run engine tests: `make test-engine`
